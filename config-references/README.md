# config-references 目录说明

本目录文件来自外部参考样例，仅用于人工对比与思路吸收。

## 使用边界

- 不作为项目运行依赖。
- 不会被 `scripts/generate_clash_rules.py` 直接读取。
- 不作为“默认前提”直接写入项目策略。

## 正确用法

1. 先在项目原则下判断是否属于通用高价值场景。
2. 仅把确认有效的条目合并到项目可控配置（例如生成器基线常量）。
3. 合并后必须通过本仓库生成与检查流程验证。
